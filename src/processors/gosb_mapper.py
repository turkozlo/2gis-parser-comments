import re

# Паттерны для определения ГОСБ (региона)
PATTERNS = {
    "Хабаровский край": r"(?i)\b(?:хабаровск|комсомольск[-\s]?на[-\s]?амуре|николаевск[-\s]?на[-\s]?амуре|советск\w*\sгаван\w*|амурск)\b",
    "Приморский край": r"(?i)\b(?:приморск\w*|владивосток|уссурийск|находк\w*|арсеньев|артем|лесозаводск|большой\sкамень|дальнереченск|партизанск|фокино)\b",
    "Сахалинская область": r"(?i)\b(?:сахалин\w*|южно[-\s]?сахалинск|корсаков|холмск|курильск\w*)\b",
    "Еврейская автономная область": r"(?i)\b(?:евре\w*|биробиджан|облучье|еао)\b",
    "Амурская область": r"(?i)\b(?:амурск\w*|благовещенск|свободный|тында|зея|шимановск)\b",
    "Чукотский автономный округ": r"(?i)\b(?:чукотск\w*|чукотка|анадыр\w*|певек|беринговск)\b",
    "Камчатский край": r"(?i)\b(?:камчатск\w*|петропавловск[-\s]?камчатск\w*|елизово|вилючинск)\b",
    "Магаданская область": r"(?i)\b(?:магадан\w*|сеяха)\b"
}

def get_gosb(city):
    """
    Определяет ГОСБ (регион) по названию города.
    
    Args:
        city (str): Название города
        
    Returns:
        str: Название региона или "Не определено"
    """
    if not isinstance(city, str):
        return "Не определено"
    
    city = city.strip()
    
    for region, pattern in PATTERNS.items():
        if re.search(pattern, city):
            return region
    
    return "Не определено"
